# ATLASBUHO — SURGICAL PASS 5

## Autodenomination Counting Integrity / 474-Row Exact Reconciliation

## OBJECTIVE

The latest implementation reports:

```text
SourceAutodenominations:
    imported      = 422
    quarantined   = 62
    documentary  = 474

422 + 62 = 484
```

Therefore the current implementation is NOT valid yet.

The documentary baseline is 474 source appendix rows, but the importer currently produces 484 outcome counts.

The immediate objective is NOT to modify counters until the number becomes 474.

The objective is to prove that every valid appendix source row has exactly ONE terminal outcome:

```text
IMPORTED
OR
QUARANTINED
```

and NEVER both.

This is a forensic reconciliation problem, not merely a counting problem.

---

# ABSOLUTE RULE

Do not trust the current explanation:

> "6 known excess plus 4 additional double-counts."

That is currently only a hypothesis.

Prove it from actual source-row identity and execution flow.

Do not declare H-111/H-115/H-116 complete until the reconciliation is demonstrated.

---

# PHASE 1 — READ THE CURRENT IMPLEMENTATION

Before editing anything:

1. Read the complete current `InaliCatalogImporter.cs`.
2. Read all related entities:

   * `SourceAutodenomination`
   * `ImportQuarantine`
   * `CatalogVersion`
   * relevant source/document entities.
3. Read the migration adding:

   * `RawDataHash`
   * unique index.
4. Read all tests covering:

   * autodenominations
   * quarantine
   * idempotency
   * documentary baselines.
5. Read the current audit documentation.
6. Identify the exact code path for:

```text
ImportAutodenominationsAsync
```

and every method it calls.

Do NOT edit before completing this read.

---

# PHASE 2 — DEFINE THE UNIT BEING COUNTED

Determine exactly what "474 documentary autodenomination rows" means.

Identify the source-row identity.

It must be possible to uniquely identify every source appendix row using deterministic source provenance, for example:

```text
SourceDocumentId
SourcePage
SourceRow / source ordinal
Source text fields
RawDataHash
```

Do not assume duplicate text means duplicate source row.

Two identical rows on different source pages are NOT automatically the same documentary row.

Two identical rows on the same page are NOT automatically the same row unless the source identity proves it.

Document the exact identity key currently used.

---

# PHASE 3 — BUILD A ROW-BY-ROW RECONCILIATION LEDGER

For the clean import, create an in-memory diagnostic ledger containing EXACTLY ONE record for every one of the 474 documentary appendix rows.

Minimum fields:

```text
SourceRowIdentity
SourceDocumentId
Page
RawDataHash
SpanishName
IndigenousName
Grouping
ResolutionMethod
TerminalOutcome
CatalogRecordId
QuarantineId
```

Where:

```text
TerminalOutcome =
    IMPORTED
    QUARANTINED
```

The ledger MUST enforce:

```text
one source row -> exactly one terminal outcome
```

If a source row reaches both outcomes:

```text
FAIL
```

If a source row reaches neither:

```text
FAIL
```

If the same source row reaches the same outcome more than once:

```text
FAIL
```

Do not hide this with `Distinct()`.

Do not hide this with `GroupBy()`.

Do not simply decrement a counter.

Find the execution path responsible.

---

# PHASE 4 — TRACE EVERY COUNTER MUTATION

Search the entire importer for every mutation of:

```text
imported count
quarantined count
autodenomination counters
artifact counters
```

For every increment:

```text
++
+=
Count
Add
```

document:

```text
source condition
counter
location
call stack
```

Specifically identify whether one appendix row can execute:

```text
imported++
```

and later:

```text
quarantined++
```

or:

```text
quarantined++
```

more than once.

Also identify whether an imported row can be counted again through:

* fallback matching
* variant matching
* group matching
* duplicate detection
* quarantine persistence
* retry paths
* recursive/import helper calls.

---

# PHASE 5 — PROVE THE "6 DUPLICATE ROWS" CLAIM

The report currently claims:

> 6 known excess from duplicate appendix rows.

Do not accept this statement without evidence.

Produce an exact table:

| Source Row ID | Page | RawDataHash | Text | Duplicate Of | Outcome |
| ------------- | ---: | ----------- | ---- | ------------ | ------- |

For every duplicate candidate, determine whether it is:

```text
TRUE DUPLICATE SOURCE ROW
OR
LEGITIMATE DISTINCT DOCUMENTARY ROW
```

The source page and source-row identity are mandatory.

If six rows are genuinely duplicates, prove exactly why.

Do not remove them from the 474 baseline merely because their text is identical.

---

# PHASE 6 — FIND THE OTHER FOUR

The report claims:

> 4 additional double-counts in matching logic.

This must be demonstrated.

Instrument the import flow so every appendix row produces an execution trace:

```text
ROW
 -> preprocessing
 -> matching
 -> candidate count
 -> resolution
 -> import/quarantine decision
 -> persistence
 -> counter mutation
```

Find the exact four rows that generate more than one terminal accounting event.

For each:

```text
SourceRowIdentity
first accounting event
second accounting event
exact method
exact source line
reason
```

Do NOT classify them as "double-counts" until the two accounting events are shown.

---

# PHASE 7 — ENFORCE ONE-ROW-ONE-OUTCOME

Refactor the accounting model if necessary.

The importer should conceptually produce:

```text
IReadOnlyList<AutodenominationImportResult>
```

where each source row returns exactly one result:

```text
Imported
Quarantined
```

Then derive counters from the result ledger:

```text
ImportedCount =
    results.Count(x => x.Outcome == Imported)

QuarantinedCount =
    results.Count(x => x.Outcome == Quarantined)
```

Do NOT maintain independent mutable counters in multiple branches if they can diverge.

The accounting source of truth should be the per-row result set.

If the existing architecture cannot be changed safely, implement the smallest equivalent mechanism that guarantees one terminal outcome per source row.

---

# PHASE 8 — SEPARATE DOCUMENTARY DUPLICATES FROM ACCOUNTING DUPLICATES

These are NOT the same:

### Documentary duplicate

Two source rows contain equivalent data.

### Accounting duplicate

The same source row generates more than one import/quarantine event.

The current bug may involve one or both.

Document them separately.

Never solve an accounting bug by deleting legitimate documentary rows.

---

# PHASE 9 — QUARANTINE PERSISTENCE

Verify:

```text
ImportQuarantine.RawDataHash
```

and its unique index.

Determine whether the quarantine hash identifies:

```text
source row
```

or merely:

```text
source content
```

This distinction matters.

If two distinct source rows contain identical content but are on different pages, a content-only unique hash could incorrectly collapse them.

Therefore explicitly answer:

> Is `RawDataHash` a source-row identity or merely a content hash?

If necessary, define a deterministic quarantine identity that includes provenance.

Do NOT weaken idempotency.

---

# PHASE 10 — BASELINE RECONCILIATION

The final clean run MUST demonstrate:

```text
Documentary source rows:
    474

Imported:
    X

Quarantined:
    Y

X + Y:
    474
```

Additionally:

```text
X >= 0
Y >= 0
```

and:

```text
every one of the 474 source rows
    has exactly one outcome
```

Do not accept a passing aggregate count without the row-level ledger.

---

# PHASE 11 — SECOND AND THIRD IMPORT

After the first clean import passes:

Run the exact same import:

### Run 1

Clean database.

### Run 2

Same source.

### Run 3

Same source.

Compare:

```text
SourceFamilies
SourceGroups
SourceVariants
SourceAutodenominations
ImportQuarantines
CatalogRecords
SourcePages
CatalogVersion
```

The database state must remain deterministic.

Specifically verify:

```text
no new CatalogRecords
no new SourceAutodenominations
no duplicate quarantines
no changed RawDataHash
no changed source provenance
```

unless the architecture explicitly defines an expected immutable audit record.

---

# PHASE 12 — FAILURE / ROLLBACK TEST

Verify that a failed validation:

```text
does not leave partial imported rows
does not leave partial quarantine rows
does not leave an incorrect CatalogVersion
```

After rollback, query:

```text
SourceFamilies
SourceGroups
SourceVariants
SourceAutodenominations
ImportQuarantines
CatalogRecords
SourcePages
CatalogVersions
```

and document the exact state.

---

# PHASE 13 — H-115 DOCUMENTATION

Only after the implementation passes:

Regenerate the audit documentation.

Do not manually edit the report to make it look consistent.

The documentation must be generated from the verified implementation/results and must report the actual values:

```text
documentary variants
imported variants
quarantined variants

documentary autodenominations
imported autodenominations
quarantined autodenominations
```

The report MUST NOT say:

```text
474 baseline satisfied
```

unless:

```text
imported + quarantined = 474
```

has been demonstrated.

---

# PHASE 14 — TEST REQUIREMENTS

Add or update tests for:

### Test A — Exact baseline

```text
474 source rows
```

must produce:

```text
imported + quarantined = 474
```

### Test B — One-row-one-outcome

Every source row has exactly one outcome.

### Test C — Duplicate text

Identical text on different pages remains distinct when provenance indicates distinct rows.

### Test D — True duplicate

A truly duplicated source row follows the documented deterministic rule.

### Test E — Ambiguous match

Ambiguous matching produces exactly one quarantine outcome.

### Test F — Idempotency

Second and third imports do not create duplicate data.

### Test G — Rollback

Failed validation leaves no partial import state.

### Test H — Quarantine identity

Distinct source rows with identical content do not collide incorrectly.

---

# PHASE 15 — DO NOT PATCH THE NUMBER

Absolutely prohibited:

```text
if (count > 474)
    count = 474;
```

or:

```text
quarantinedCount -= 10;
```

or:

```text
Distinct()
```

used merely to hide multiple accounting events.

or:

```text
Skip()
```

used merely to make the baseline pass.

or deleting rows from the source dataset without documentary evidence.

The final aggregate must be the mathematical consequence of correct row-level processing.

---

# PHASE 16 — REQUIRED FINAL REPORT

Return:

## 1. Root Cause

Exact source of:

```text
422 imported
62 quarantined
484 accounting events
```

## 2. Exact 10 Excess Events

List every excess event.

For each:

```text
SourceRowIdentity
page
hash
outcome
duplicate event
method
source location
```

## 3. Corrected Accounting Model

Explain how one-row-one-outcome is enforced.

## 4. Final Baseline

```text
Documentary = 474
Imported = X
Quarantined = Y
X + Y = 474
```

## 5. Database Verification

Provide actual MySQL query results.

## 6. Repeatability

Provide Run 1 / Run 2 / Run 3 comparison.

## 7. Rollback

Provide actual rollback evidence.

## 8. Tests

Provide:

```text
dotnet test
```

actual output.

Also provide:

```text
dotnet build -c Release
```

actual output.

Do not summarize as "passed" without showing the relevant result.

## 9. Documentation

Confirm H-115 is regenerated from the final verified implementation.

## 10. Git

Report:

```text
branch
base SHA
final SHA
git status
commit
push result
```

Do not claim pushed unless the push actually succeeded.

---

# PHASE CLOSURE RULE

Do NOT declare:

```text
PHASE 1 CLOSED
```

yet.

Current state:

```text
PHASE 1 — CONTINUE REQUIRED
```

The phase may become:

```text
CLOSED / VERIFIED
```

only after:

1. `474 = imported + quarantined`
2. Every source row has exactly one terminal outcome.
3. No accounting duplicate remains.
4. Documentary duplicates are explicitly reconciled.
5. Second and third imports are deterministic.
6. Rollback is verified.
7. H-115 documentation matches the implementation.
8. Tests pass.
9. Release build passes.
10. Git evidence is actual and reproducible.

Until then:

```text
NOT CLOSED.
```

Do not optimize for a green report.

Optimize for a reproducible forensic data lineage.
