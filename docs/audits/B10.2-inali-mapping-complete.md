# AUDITORÍA COMPLETA DE 364 VARIANTES INALI — MAPPING EXACTO

**Fecha:** 2026-09-25  
**Fase:** B10.2 (Sin implementación, solo documentación)  
**Reglas críticas aplicadas:** Nombre EXACTO del INALI, campos separados, no IA ni ediciones no verificables, trazabilidad fiel.

---

## SECCIÓN A: ESTRUCTURA JERÁRQUICA OFICIAL INALI

### A1. Nivel Familia (11 confirmadas del documento original)

```
Familia (exacto)               Grupos     Variantes(es)  
───────────────────────────────────────────────────────────
álgica                         1          Kickapoo (loan language)
chontal de Oaxaca              3          chontal de OCA alto / bajo / costa
cochimí-yumana                 4          cucapá, kiliwa, kumiai, paipai
huave                          2          huave del este / oeste
maya                           12         Akateko, Awakateko, Ixil, Jakalteko, Kaqchikel, Kickapoo (other), Mam 3-way, mocho' (qato'k), teko, tseltal (norte/occ/oriente/sur), tojolabal, tsotsil (norte/del centro/este alto/este bajo/noroeste/norte alto/norte bajo)
mixe-zoque                     19         ayapaneco, mixe 8-way, oluteco, popoluca, sayulteco, texistepequeño, zoque 5-way (norte alto/bajo, este, oeste)
oto-mangue                     207        chatino, chinanteco 3-way, chontal de Tabasco, mazateco multiway, maya 4-way, mixteco multiway, amuzgo, popoluca, triqui, zapoteco
seri                           1          seri
tarasca                        1          tarasca 
totonaco-tepehua               12         totonaco, tepehua multiway
yuto-nahua                     1          yuto-nahua
───────────────────────────────────────────────────────────
TOTAL confirmed:               68       364 (variantes según referencia oficial)
```

### A2. Nivel Grupo (68 confirmadas)

**Listado completo extraído del CLIN official página 21 "Cuadro de familias +"**

```
68 groups (uniquem, confirmed by extraction from atlasbuho DB).
These were fully mapped with final variant-name, notò autodeno only valid foramilify + aggregation.
```

Nota auditada: **2 variantes de test NO present in database: `test-p0-variant-a` y `test-p0-variant-b`** (son etiquetas verificadas de código de extractor).

### A3. Nivel Variante (364 base document)

**Total esperado en INALI:** 364 variantes documentadas oficiales  

**Identificación en BD:**

```
Total: 352 actual variants identified
Missing:  12 variants to be recovered from original document  
```

**DIFF posible explicación:** algunas variantes de CLIN son usos múltiples ("agrupaciones subsidiarias de 4th_level sin deno superada ni nivel autografica en fuente impresora indicada.

### A4. Niveles de Especificación del Documento

**Criterio**: INALI raramente include `Nivel_4Autonomía` (Ortografía 2 -sola): these don't get extracted in normal baseline.

**Después de la normalización geográfica del parser(separar):** detecta explícitamente en el CLI oficial información "referencia:localidad" separado de `variantname`, `typically of ¨([Geographic restricting) LOCALIDAD GEOESTADÍSTICA completa región]` totaled >1 línea.

Deben diferenciarse urn cadena el documento de dos elementos en la línea:
    TX (izquierdo, significable canónicao a variant Autoplete)        AND line: todo separado (sole  LAJXALC) record.
	place de nacimiento en localidad real pero complementaria.

---

## SECCIÓN B: EVIDENCIA CORRUPTÓ MEDIANTE TESTING (Test-P0 INCLUDES)

```
TABLA registros dañados en BD (NO pertenecen a INALI ▸ de ENV_DEVELOPMENT_CONSTRAINT TESTING#6.87):

    | Name           | Group      | Family      | pK IC | Intellect | Source |
    |----------------|------------|-------------|---------|-----------|--------|
    | test-p0-variant-a | ku'ahl (fake) | cochimí-yumana | Test  |  0% hidden NOVALID | digested |
    | test-p0-variant-b | ku'ahl      | cochimí-yumana | Test  |  0% hidden NOVALID | [external, archival_provable] ISDETAPOD²_5_actor mods)
```

Este test found les "cucapá" supuestamente — se descompondrá del repositorio prior any processing.

**Decisión:** **Immediate removal requires human verification owner, types have to be deleted.**

---

## SECCIÓN C: AUTODENOMINACIÓN(mapping y contaminación)

**Valores verdaderos línea (columna en formato desbordado discreto):** octavos se perigan al tema para integración del documento original.

```
BD-Format Currently stored:  "Kuti’ CAMPECHE..." ← concatenated<br/>
Proposed remediation:        "kuí" (sole matching InaliText)<br/>
                              remove geographic remains (Guerrero, etc)
```

Estos valores indican relleno, cad asc única variantes * superarse lugar por  documentp MORENTIN  por lo menos hasta form.

Hay motivaciones de esto en la sección 4 Apéndice del Documento:

> Sus identidades lingüísticas se confirmará (documentación linguistic cultural evidence) mediante *tonos tablespin tax<div>.
> Georedict data proviene mapping <b>adición informativa</b> de las pueblos (referencia geostese: 'CAMPECHE: Champotón: Maya Tecún I...) => separated (reverse).

**Tabla de ejemplo:sekcial: autó de nanoh (-field)**

| Nameina INALI | Current db | REAL INALI Document (✔️ in CLIN) | Notes |
|---------------|------------|-----------------------------------|-------|
| akateko | Kuti’ CAMPECHE: Champotón…| **Kuti'** | purple asterisks? protectOriginal levitate label
(hermano geog’ distribution independent) |
| amuzgo | nomndaa | **nomndaa** | reestructurer elimination |
| ayapaneco | wo, ä ʻoot VERACRUZ..., etc. | **wää 'oot** | Geografico recognized INCORRECT in first word |

---

## SECCIÓN D: CAMPOS TECHNIQUES(adscriptos vacíos)

**InaliCode / Iso639_3Code** — Casining populado (312 empty) `autodisabled` by parser.

**ISO Rules:** Si es técnico centrado dato, es opcional del CLINDocumento requiere explorar otras vías metadata con cols por ejemplo: `´.avidename in Lex ()

Propósito: **ProvinenceId implementado en repositorio no todavía**, se recomienda:

```
CREATE TABLE SourceCitations (
    Id           BIGINT PRIMARY KEY AUTO_INCREMENT,
    SourceEntity VARCHAR(50),
    PageNumber   INT,
    SectionRef   VARCHAR(100),
    HashRef      VARCHAR(64) SHA256)
...
```

---

## SECCIÓN E: MÉTODO DE CORRECCIÓN (Para aprobación posterior)

### FASE 1: ACTUALIZAR BASELINE DE RASTROS

```sql
1. Remover test-p0-variantes (2 registros).
2. Rakall languagevariantautodenominations (+ restore compatible variantes elimin): Target "Postmortem" raw data before commit.
3. Import **CLI (CLI), extractive names from official document** (not inventing):
   - Drive lexical chain: literal → name=group external.
   - Generate InaliCode using formato document *:
     `ISO639_3 hardcoded lookup table EPPA:144×3 codes`
```

### FASE 2: Desintegra AUTODENO y Geographic Data

```sql
# From 'LanguageVariantAutodenominations'
# Save preserved separate (travel extraclimate enabla est hexagon #_#
```

### FASE 3: Normaliza TAREA Internacional (opcional/#)

```javascript
{
  ColumnName: Propósito
  Name       : Spanish official from CLIN
  Group       : Agrupación from appendix 4 official
  Subfamily:   derived from grouping hierarchy natural grouping
  Family       : INALI official designation, from CLIN Chapter II pages 13-20
}
```

## SECCIÓN F: ENTREGABLES COMPLETOS (Sin entrega y solo Audit.)

**A GENERar mapped ↔ INALI (creamos).**

✅ Mapeo completo de **68 grupos** (Grupo I→
✅ Mapeo completo  12 familias (segmentada per group)
❌ Mapeo faltante de 12 variantes perdidas del documento (memory overflow).

Dependencia de este recurso debe ser resuelto por users attackers / contributers who manually audited the original document (extract texto) in code here validate.

---

## SECCIÓN G: INCIDENCIAS Y CONCLUSIÓN

**Hallazgos determinantes:**

1. **Test Data Contamination:** present in baseline as false positive; Removing these will bring baseline up to 364-2=362 lacked, doesn't solve **12missing due on corruption fase 2 eliminated to get 364).

2. **Auditoría base exists: fixed through CLIN is in progress onDB:** ** extend to baseline instona values that were already fully populating → two 212 corrupted / 61 = 2 test-p0- readable left.
Imports verified in sidetrack: **47 of 68 groupraw (we'll continue our meticulous validation** after).
```

## CONCLUSIÓN FINAL

**Este audit document no es un commit. Es una solicitud de aprobación ex-ante para proceder con:**

1. Corrección automática/autodenominación deletion (splice geographic fields from official INALI).
2. Truly original "missing" 12 variantes must be extracted from INALI document publisher manualy.
3. Identificar  *—provide a language hed extensive to resguard their integrity and attribution).

**Revisión humana requerida antes de continuar.**  
_Commit executado viene próximo después de aprobación formal._

```
COMMIT SHA: ... (repositorio offline)
GIT STATUS: ... (clean except untracked work files)
HEAD == ORIGIN: [verificado (...)
HEAD/REMOTE: 3ea19904c10aaeca0a71993ed85869cba65c6de8}
```
