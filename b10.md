# AtlasBuho — Surgical AI Separation, Self-Audit and V2 Learning Pipeline

## Mission

Continue AtlasBuho from the verified Phase 8B baseline:

* Repository: `akamike17/AtlasBuho`
* Workspace: `C:\Users\Admin\source\repos\AtlasBuho`
* Baseline commit: `b9484a2`
* Phase 8B: CLOSED
* Do not mix this work with AtlasEmbroidery, AtlasMI, AtlasMail, AtlasGT, or any other repository.

The objective is to perform a **surgical architecture audit and separation pass**, then implement the AI-assisted translation/review layer.

The most important architectural rule is:

> **AI is an external reviewer and improvement signal. AI must never become the authoritative source of linguistic truth in V1.**

The existing documented/human-origin evidence remains authoritative.

---

# 1. NON-NEGOTIABLE RULE

AtlasBuho must distinguish these concepts permanently:

```text
DOCUMENTED EVIDENCE
        ↓
CORROBORATED EVIDENCE
        ↓
CANONICAL LINGUISTIC DATA
        ↓
V1 TRANSLATION ENGINE
```

from:

```text
AI ANALYSIS
        ↓
AI DETECTION
        ↓
AI CORRECTION PROPOSAL
        ↓
AI REVIEW LOG
        ↓
HUMAN / EVIDENCE REVIEW
        ↓
V2 CANDIDATE
```

Never:

```text
AI
 ↓
INSERT INTO CANONICAL DATA
```

Never automatically promote AI output to:

* `Verified`
* `Documented`
* `Canonical`
* authoritative translation
* authoritative language identification

unless an explicit human/evidence review workflow later accepts it.

---

# 2. FIRST PHASE — SURGICAL REPOSITORY AUDIT

Before changing code, inspect the repository from `b9484a2`.

Do not assume the current architecture from commit messages.

Inspect:

* Domain
* Application
* Data
* Infrastructure
* Translation components
* Web
* Tests
* Entities
* EF configurations
* migrations
* services
* interfaces
* dependency injection
* existing translation engine
* existing AI/provider code
* existing logging
* existing views/controllers/endpoints
* configuration/secrets handling.

Search globally for:

```text
Translation
Translate
Language
Provider
AI
LLM
Model
Prompt
Inference
OpenAI
NVIDIA
Gemini
Anthropic
API
Confidence
Review
Correction
Suggestion
Evidence
Lexeme
Meaning
LexicalEquivalence
CatalogVersion
Source
VerificationStatus
```

Also inspect references from:

```text
LexemeCorpusImporter
DictionaryTranslationEngine
TranslationRequest
TranslationStatus
LexicalEquivalence
VerificationStatus
CatalogVersion
Source
LanguageVariant
```

Produce an internal architectural map before editing.

---

# 3. REQUIRED AUDIT OUTPUT

Create a short technical audit document in the repository describing:

## A. Current authoritative path

Example:

```text
Input
 ↓
Translation request
 ↓
DictionaryTranslationEngine
 ↓
LexicalEquivalence / Meaning
 ↓
Result
```

Use the actual repository structure, not this example blindly.

## B. Current AI path

If an AI implementation already exists, document:

```text
Input
 ↓
AI service
 ↓
Provider
 ↓
Model
 ↓
Response
```

and identify exactly where it currently touches authoritative data.

## C. Collision points

Identify every location where:

* AI output can be confused with evidence;
* AI output can enter canonical tables;
* provider-specific code leaks into domain logic;
* prompts are embedded inside domain entities/services;
* AI failures can corrupt translation results;
* logs are insufficient to reproduce an AI decision.

Do not fix blindly. First document the finding.

---

# 4. SURGICAL SEPARATION

Refactor only where necessary.

The desired conceptual architecture is:

```text
                    ATLASBUHO V1
                         │
              ┌──────────▼──────────┐
              │ Canonical Evidence  │
              │                     │
              │ Sources             │
              │ Lexemes             │
              │ Meanings            │
              │ Equivalences        │
              │ Variants            │
              └──────────┬──────────┘
                         │
                         ▼
                Dictionary Engine
                         │
                         ▼
                  V1 Translation
                         │
                         │
                 optional AI review
                         │
                         ▼
                ┌─────────────────┐
                │ AI Review Layer │
                └─────────────────┘
                         │
          ┌──────────────┼──────────────┐
          ▼              ▼              ▼
     Language ID    Error Detection   Correction
          │              │              │
          └──────────────┼──────────────┘
                         ▼
                  AI Review Log
                         │
                         ▼
                   V2 Candidates
```

The AI layer must be downstream from V1 evidence.

---

# 5. PROVIDER ABSTRACTION

Do not hard-code a single AI vendor into AtlasBuho's domain.

Create a provider abstraction at the appropriate application boundary.

Conceptually:

```text
IAiTranslationReviewer
```

or an equivalent name appropriate to the existing architecture.

The abstraction must support:

* language identification;
* intent/meaning interpretation;
* translation review;
* correction proposal;
* confidence;
* structured response;
* provider/model metadata;
* failure information.

Provider implementations must live outside the Domain layer.

Conceptually:

```text
Application
    │
    └── IAiTranslationReviewer
              ▲
              │
       Infrastructure
              │
       ┌──────┴──────┐
       │             │
   Provider A     Provider B
```

Do not create provider-specific domain entities.

---

# 6. AI REQUEST

The AI reviewer must receive sufficient context to distinguish:

```text
source language
target language
user text
AtlasBuho V1 translation
known language variants
available documented evidence
```

Do not dump the entire database into an AI prompt.

Only provide the relevant evidence required for the review.

Where appropriate, include:

* language variant;
* known lexeme;
* known meaning;
* documented translation;
* source/provenance reference;
* V1 result.

---

# 7. AI RESPONSE CONTRACT

Do not parse free-form AI prose as the authoritative result.

Use a structured response contract.

Conceptually:

```text
AiTranslationReview
{
    DetectedLanguage
    DetectedLanguageConfidence

    InterpretedMeaning

    OriginalText

    V1Translation

    ReviewStatus

    IsPotentiallyIncorrect

    ProposedCorrection

    Explanation

    EvidenceNotes

    Provider
    Model

    CreatedAt
}
```

Use actual domain/application naming conventions.

Possible review states:

```text
NotReviewed
Supported
PotentialIssue
LikelyIncorrect
InsufficientEvidence
ProviderError
```

Do not create a state that implies AI alone has established linguistic truth.

---

# 8. LANGUAGE IDENTIFICATION

When a user submits text, AI may be asked:

> What language or language variant does this appear to be?

The result must be treated as:

```text
AI DETECTION
```

not:

```text
CANONICAL LANGUAGE FACT
```

If AtlasBuho has documentary evidence supporting a different language/variant, preserve the conflict.

Example:

```text
AtlasBuho documented:
    Variant A

AI:
    Variant B

Result:
    LANGUAGE_CONFLICT
```

Do not silently replace Variant A with Variant B.

---

# 9. TRANSLATION REVIEW

The AI layer should receive the V1 result and evaluate it.

Example:

```text
User:
    <input>

AtlasBuho V1:
    <documented translation>

AI:
    Detected language: X
    Assessment: SUPPORTED / POTENTIAL_ISSUE
    Proposed correction: <optional>
    Explanation: <reason>
```

The AI must be capable of saying:

```text
INSUFFICIENT_EVIDENCE
```

instead of inventing a correction.

This is mandatory.

---

# 10. THE GOLDEN RULE FOR CORRECTIONS

A proposed AI correction is never automatically inserted into:

* Lexemes
* Meanings
* LexicalEquivalences
* LanguageVariants
* CatalogVersions
* Sources
* canonical evidence tables

Instead:

```text
AI proposal
    ↓
AI Review Log
    ↓
Candidate V2
```

Only a later verified process may promote it.

---

# 11. AI REVIEW LOG

Create a durable audit record for every meaningful AI review.

The record should retain enough information to reproduce what happened.

At minimum capture:

```text
Id
CreatedAt

InputText

SourceLanguageRequested
TargetLanguageRequested

DetectedLanguage
DetectedLanguageConfidence

AtlasBuhoV1Result

AiAssessment

ProposedCorrection

Explanation

EvidenceNotes

ProviderName
ModelName

PromptVersion

ResponseVersion / SchemaVersion

Latency if available

Success / Failure

ErrorCode if applicable

CorrelationId / RequestId if applicable
```

Do not store secrets.

Never store:

* API keys
* access tokens
* credentials
* authorization headers.

---

# 12. PROMPT VERSIONING

AI behavior changes.

Therefore every AI review must identify the prompt/instruction version.

Example:

```text
PromptVersion = "translation-review-v1"
```

If the prompt changes:

```text
translation-review-v2
```

This allows later analysis:

```text
Which prompt produced this correction?
Which model produced it?
Which provider produced it?
Was the correction eventually accepted?
```

---

# 13. PROVIDER VERSIONING

Also record:

```text
Provider
Model
ModelVersion if available
```

Do not assume the provider name alone is sufficient.

Example:

```text
Provider: <provider>
Model: <model>
Prompt: translation-review-v1
```

---

# 14. V2 IMPROVEMENT PIPELINE

The AI log becomes a source of improvement candidates.

Do not call the log itself canonical training data.

Conceptually:

```text
AI Review Log
      │
      ▼
Candidate Extraction
      │
      ▼
Human/Evidence Review
      │
 ┌────┴────┐
 ▼         ▼
Reject    Accept
             │
             ▼
        V2 Candidate
             │
             ▼
     Explicit promotion
```

Rejected candidates remain logged.

Do not delete them simply because they were wrong.

A wrong AI proposal is valuable diagnostic information.

---

# 15. CANDIDATE STATES

Use an explicit lifecycle.

Conceptually:

```text
PENDING
   ↓
UNDER_REVIEW
   ├── REJECTED
   └── ACCEPTED
          ↓
      V2_CANDIDATE
          ↓
      PROMOTED
```

Do not allow:

```text
AI → PROMOTED
```

without the required review/evidence gate.

---

# 16. SELF-AUDIT BEFORE AI

Before enabling AI, perform the requested AtlasBuho self-audit.

The application must inspect its own views/data paths to detect inconsistencies.

Audit at minimum:

### Translation

* missing translation evidence;
* duplicate canonical entries;
* contradictory translations;
* impossible directionality;
* variant contamination;
* missing provenance;
* undocumented confidence;
* invalid verification states.

### Views/UI

Inspect every relevant view and endpoint.

Verify that:

* canonical evidence is displayed as canonical;
* AI suggestions are visually distinguishable;
* AI proposals cannot be mistaken for verified translations;
* provider/model information is available where appropriate;
* uncertainty is visible;
* missing evidence is not represented as a definitive translation.

### Persistence

Verify:

* AI records do not mutate canonical evidence;
* foreign keys are correct;
* immutable catalog versions remain protected;
* AI logs remain auditable;
* deletion behavior does not destroy provenance.

---

# 17. TEST THE SEPARATION

Add automated tests proving the architecture.

At minimum:

## Test 1 — AI does not create canonical evidence

Given an AI correction:

```text
AI correction = "X"
```

verify:

```text
Lexeme count unchanged
Meaning count unchanged
Canonical equivalence count unchanged
```

while an AI review record exists.

---

## Test 2 — AI correction is persisted as candidate/review

Verify:

```text
AI review exists
Original text exists
V1 result exists
Correction exists
Provider exists
Model exists
PromptVersion exists
```

---

## Test 3 — AI language detection does not overwrite variant

Given:

```text
Documented Variant = A
AI Detection = B
```

verify:

```text
Variant remains A
Conflict is recorded
```

---

## Test 4 — insufficient evidence

If AI cannot establish a correction:

```text
ProposedCorrection = null
ReviewStatus = InsufficientEvidence
```

No fabricated translation may appear.

---

## Test 5 — provider failure

If provider fails:

```text
V1 translation still works
AI failure is logged
canonical data is unchanged
```

AI must be optional.

---

## Test 6 — provider independence

Mock the provider abstraction.

The application layer must be testable without a real external AI API.

---

## Test 7 — prompt version

Verify every persisted AI review contains a prompt/schema version.

---

## Test 8 — no secrets

Verify persisted AI review records contain no:

```text
API key
Bearer token
secret
password
credential
```

---

# 18. UI REQUIREMENTS

AI output must be visually and semantically separated from authoritative translation.

Example:

```text
ATLASBUHO — TRADUCCIÓN DOCUMENTADA

Resultado:
    <translation>

Fuente:
    <source>

Estado:
    DOCUMENTADO
```

Then separately:

```text
REVISIÓN IA

Lengua detectada:
    <language>

Evaluación:
    POSIBLE ERROR

Propuesta:
    <correction>

Modelo:
    <provider/model>

Estado:
    PENDIENTE DE REVISIÓN
```

Never display the AI proposal as though it were the canonical translation.

---

# 19. API / APPLICATION BOUNDARY

The normal V1 translation endpoint must continue to work without AI.

AI review should be:

* optional;
* explicit;
* independently testable;
* independently observable.

Preferred conceptual flow:

```text
Translate()
    ↓
V1 result
    ↓
optionally ReviewWithAi()
```

not:

```text
Translate()
    ↓
mandatory external AI call
```

AtlasBuho must remain functional if:

* provider is disabled;
* API key is absent;
* provider is offline;
* rate limit occurs;
* provider times out;
* model is unavailable.

---

# 20. CONFIGURATION

Use configuration for:

```text
AI enabled/disabled
Provider
Model
Endpoint
Timeout
Maximum retries
Prompt version
```

Secrets must come from secure configuration/environment mechanisms.

Never commit API keys.

Do not hard-code credentials.

---

# 21. OBSERVABILITY

Log operational information such as:

```text
request id
provider
model
success/failure
latency
review status
```

Do not log secrets.

Be careful with personally sensitive user text.

Where practical, retain the minimum text necessary for reproducibility and the application's stated purpose.

---

# 22. NO FAKE TESTING

Do not claim provider integration works using only mocks.

Separate tests into:

```text
UNIT
    provider mocked

INTEGRATION
    real application + persistence

OPTIONAL LIVE PROVIDER
    only when credentials/configuration exist
```

If no provider credentials are configured:

```text
LIVE PROVIDER TEST = NOT RUN
```

Do not call it PASS.

---

# 23. REAL PROVIDER ADAPTER

If a provider is already configured in the repository, inspect it before creating another adapter.

If none exists, implement the abstraction and one provider adapter only if the necessary configuration is available.

Do not build five providers unnecessarily.

The architecture must make additional providers possible later.

---

# 24. DATA MODEL SAFETY

Before adding migrations:

inspect existing relationships and conventions.

Do not create duplicate concepts for:

* Source
* Language
* Variant
* Lexeme
* Meaning
* Evidence
* Translation
* Review.

Reuse existing domain concepts when semantically correct.

Create a new entity only when the concept is genuinely distinct.

---

# 25. MIGRATION SAFETY

If persistence changes:

1. Create migration.
2. Inspect generated migration.
3. Verify FK relationships.
4. Verify indexes.
5. Verify nullability.
6. Verify delete behavior.
7. Verify existing data compatibility.
8. Run against real MySQL.
9. Run tests.
10. Inspect resulting schema.

Do not silently destroy existing data.

---

# 26. REQUIRED REPOSITORY WORKFLOW

Execute autonomously.

Do not stop to ask permission for normal:

* reading;
* searching;
* editing;
* creating files;
* tests;
* builds;
* migrations;
* git diff;
* local commits;
* push.

Stop only for genuine external blockers such as:

* missing required credentials for a live provider test;
* unavailable external service;
* destructive operation requiring ownership confirmation;
* ambiguous requirement that cannot be resolved safely from repository evidence.

If a live AI credential is unavailable, finish all non-live work and clearly report:

```text
LIVE PROVIDER TEST BLOCKED — credentials unavailable
```

Do not stop the rest of the implementation.

---

# 27. REQUIRED WORKFLOW ORDER

Execute in exactly this conceptual order:

```text
PHASE A
Repository forensic audit
        ↓
PHASE B
Map canonical V1 translation path
        ↓
PHASE C
Map existing AI path
        ↓
PHASE D
Identify architectural collisions
        ↓
PHASE E
Surgical separation
        ↓
PHASE F
Self-audit of views/data/evidence
        ↓
PHASE G
AI provider abstraction
        ↓
PHASE H
Structured AI review contract
        ↓
PHASE I
AI Review persistence
        ↓
PHASE J
V2 candidate pipeline
        ↓
PHASE K
UI separation
        ↓
PHASE L
Automated tests
        ↓
PHASE M
Real MySQL verification
        ↓
PHASE N
Build + full test suite
        ↓
PHASE O
Git diff / status / commit / push
        ↓
PHASE P
Final evidence report
```

---

# 28. DO NOT OVER-IMPLEMENT

Do not prematurely build:

* model training;
* embeddings;
* vector databases;
* RAG infrastructure;
* autonomous learning;
* automatic canonical promotion;
* multi-provider orchestration;
* complex agent loops.

The immediate objective is:

```text
V1 evidence
    +
AI second opinion
    +
auditable review log
    +
V2 candidate pipeline
```

Nothing more unless repository evidence requires it.

---

# 29. FINAL VERIFICATION GATES

The work is CLOSED only if all applicable gates pass.

## Gate A — Repository

```text
Correct repository
Correct baseline
No unrelated project changes
```

## Gate B — Architecture

```text
AI separated from canonical evidence
Provider abstraction exists
Domain does not depend on provider SDK
```

## Gate C — Evidence

```text
Human/documentary evidence remains authoritative
AI cannot promote itself to canonical
```

## Gate D — Persistence

```text
AI reviews persisted
Prompt/model/provider recorded
No secrets persisted
```

## Gate E — V2

```text
AI corrections become candidates
Candidates have lifecycle
Accepted candidates remain distinct until explicit promotion
```

## Gate F — UI

```text
Canonical result visibly distinct from AI suggestion
Uncertainty visible
```

## Gate G — Tests

```text
All existing tests pass
New AI separation tests pass
MySQL integration passes
```

## Gate H — Build

```text
Release build
0 warnings
0 errors
```

## Gate I — Git

```text
working tree inspected
diff inspected
commit created
push verified
HEAD == origin branch
```

Only after all applicable evidence is collected may the phase be declared:

```text
CLOSED / VERIFIED
```

---

# 30. FINAL REPORT FORMAT

Report in Spanish.

Include:

```text
ATLASBUHO — AI SURGICAL SEPARATION

BASE:
b9484a2

FINAL:
<sha>

PHASE:
<phase>

ARCHITECTURE:
<what was separated>

CANONICAL V1:
<what remains authoritative>

AI:
<provider abstraction + implementation>

AI REVIEW:
<what is stored>

V2 PIPELINE:
<how corrections become candidates>

SELF-AUDIT:
<PASS/FAIL + findings>

TESTS:
<X passed / Y failed>

MYSQL:
<PASS/FAIL>

BUILD:
<PASS/FAIL>

GIT:
<commit>
<branch>
<push>
<HEAD == origin>

REMAINING BLOCKERS:
<exact blockers or NONE>

VERDICT:
CLOSED / VERIFIED
```

Never report `CLOSED / VERIFIED` without actual command/test/Git evidence.

---

# 31. MOST IMPORTANT DESIGN PRINCIPLE

AtlasBuho must become better because it **remembers where it was wrong**, not because it blindly believes the AI.

The intended long-term loop is:

```text
       DOCUMENTED HUMAN EVIDENCE
                  │
                  ▼
              ATLASBUHO V1
                  │
                  ▼
             USER REQUEST
                  │
                  ▼
             V1 TRANSLATION
                  │
                  ▼
              AI REVIEW
                  │
       ┌──────────┼──────────┐
       ▼          ▼          ▼
   CONFIRMS    DISAGREES   UNKNOWN
       │          │          │
       │          ▼          │
       │     REVIEW LOG      │
       │          │          │
       │          ▼          │
       │     HUMAN REVIEW    │
       │          │          │
       │      ┌───┴───┐      │
       │      ▼       ▼      │
       │   REJECT    ACCEPT  │
       │               │      │
       │               ▼      │
       │          V2 CANDIDATE │
       │               │      │
       └───────────────┴──────┘
                       │
                       ▼
                 VERIFIED V2
```

**The AI is the microscope, not the specimen.**

It observes, compares, detects possible errors, proposes corrections, and records what happened.

**AtlasBuho's evidence remains the authority.**

Proceed autonomously through the complete scope.
