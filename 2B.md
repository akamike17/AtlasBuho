# AtlasBúho — FASE 1

# CIERRE FORENSE DEL INVENTARIO LINGÜÍSTICO INALI

## MD QUIRÚRGICO — NO AVANZAR HASTA DEMOSTRAR CADA INVARIANTE

---

# 0. MISIÓN

Estás trabajando en **AtlasBúho**.

La misión de esta intervención NO es agregar traducción, pronunciación, IA, enriquecimiento lingüístico, códigos externos, crawler ALIN ni funcionalidades de usuario.

La misión exclusiva es:

> **Cerrar técnicamente la preservación del catálogo oficial INALI como fuente patrimonial, con correspondencia demostrable entre fuente RAW → parser → modelo → MySQL, sin inventar, corregir, completar ni reinterpretar ningún dato.**

La Fase 1 NO puede declararse `CLOSED`, `VERIFIED`, `COMPLETE` ni equivalente mientras exista una discrepancia sin explicar.

---

# 1. REGLA ABSOLUTA

## PROHIBIDO

No hagas ninguna de estas cosas:

* inventar una agrupación faltante;
* inferir una agrupación solamente porque el número oficial dice 68;
* renombrar una agrupación para hacer coincidir conteos;
* fusionar agrupaciones parecidas;
* dividir una agrupación arbitrariamente;
* eliminar variantes duplicadas sin demostrar que son duplicados reales;
* modificar nombres indígenas;
* corregir ortografía del documento fuente;
* "normalizar" Unicode reemplazando el valor original;
* traducir nombres;
* completar autodenominaciones ausentes;
* obtener datos de memoria del modelo;
* utilizar Wikipedia u otra fuente para rellenar huecos del CLIN;
* introducir códigos ISO/INALI externos dentro del inventario fuente sin distinguir su procedencia;
* declarar VERIFIED porque el número final coincide;
* declarar CLOSED mientras MySQL no contenga los datos reales;
* utilizar ALIN para completar datos que pertenecen al CLIN;
* hacer crawling masivo de ALIN durante esta etapa;
* ejecutar operaciones destructivas contra la BD.

## PRINCIPIO

Si el documento dice X:

```text
source_value = X
```

se conserva X.

Si AtlasBúho necesita una representación normalizada:

```text
source_value = X
normalized_value = Y
```

Nunca:

```text
source_value = Y
```

---

# 2. ESTADO ACTUAL CONOCIDO

La auditoría anterior estableció:

```text
RAW:
catalogs/raw/inali/CLIN_completo.pdf

SHA256:
21cef44abf0d896555f26954bf319340ad4814fa8a3f0719b0d068311ff1be7c

SIZE:
3,082,863 bytes

PAGES:
256

CATALOG PAGES:
30–212

FAMILIES:
11

VARIANTS:
364

GROUPS PARSED:
67

GROUPS EXPECTED/OFFICIAL:
68

AUTODENOMINATIONS:
475
correspondientes a 364 variantes

GEO REFERENCES:
359/364

UNICODE:
tested successfully

PARSED OUTPUT:
catalogs/parsed/inali_final_catalog.json
```

La discrepancia crítica actual es:

```text
SOURCE/EXPECTED GROUPS = 68
PARSER DETECTED         = 67
```

Esto debe resolverse ANTES de continuar.

---

# 3. PRIMERA OPERACIÓN — CONGELAR EVIDENCIA

Antes de modificar parser, entidades, migraciones o datos:

1. Leer el SHA256 actual del PDF.
2. Verificar que el archivo siga siendo exactamente:

```text
21cef44abf0d896555f26954bf319340ad4814fa8a3f0719b0d068311ff1be7c
```

3. Registrar tamaño.
4. Registrar número de páginas.
5. Registrar hash del JSON actual.
6. Registrar cantidad actual de:

   * familias;
   * agrupaciones;
   * variantes;
   * autodenominaciones;
   * referencias geográficas.
7. No sobrescribir evidencia anterior.

Crear un informe de auditoría reproducible.

---

# 4. SEGUNDA OPERACIÓN — RESOLVER 67 VS 68

## ESTA ES LA PRIORIDAD MÁXIMA

No aceptes:

```text
67
```

ni:

```text
68
```

hasta demostrar exactamente de dónde sale cada agrupación.

Debes reconstruir el árbol del CLIN:

```text
FAMILIA
  └── AGRUPACIÓN
        └── VARIANTE
```

y producir una tabla determinista:

| # | Familia | Agrupación | Variantes | Página(s) | Evidencia |
| - | ------- | ---------- | --------: | --------- | --------- |

La tabla debe contener **todas** las agrupaciones detectadas.

---

# 5. AUDITORÍA BIDIRECCIONAL

No basta con que el parser encuentre 67.

Hay que hacer dos recorridos independientes.

## RECORRIDO A — PDF → parser

Extraer todos los encabezados/árboles que correspondan a agrupaciones.

Resultado:

```text
PDF_GROUPS
```

## RECORRIDO B — catálogo/documento → parser alternativo

Construir una segunda detección independiente utilizando otra señal del documento:

* encabezados;
* árbol genealógico;
* índice;
* estructura tipográfica;
* páginas;
* etiquetas de variantes;
* referencias cruzadas.

Resultado:

```text
SECONDARY_GROUPS
```

Después comparar:

```text
PDF_GROUPS
SECONDARY_GROUPS
```

y producir:

```text
INTERSECTION
ONLY_IN_A
ONLY_IN_B
```

Si ambas rutas producen 67, NO asumir que el oficial es 67.

Investigar dónde aparece la discrepancia.

---

# 6. BUSCAR LA AGRUPACIÓN PERDIDA

Investigar específicamente:

* familias con pocos grupos;
* grupos sin variantes;
* grupos extintos;
* grupos marcados con †;
* grupos mencionados solamente en árboles;
* grupos mencionados en notas;
* grupos presentes en índice pero no en páginas de catálogo;
* grupos presentes en apéndices;
* nombres alternativos;
* variantes agrupadas bajo una categoría que el parser haya interpretado incorrectamente.

IMPORTANTE:

Si una agrupación oficial aparece en el documento pero no tiene variantes vivas, debe conservarse como agrupación.

No eliminarla porque:

```text
variant_count == 0
```

No convertir:

```text
group_without_variants
```

en:

```text
missing_group
```

---

# 7. NO CORREGIR EL PDF CON CONOCIMIENTO EXTERNO

Si encuentras una posible agrupación adicional:

NO hagas:

```text
"Creo que debe ser X"
```

Debes presentar:

```text
nombre exacto
página
texto fuente
contexto
señal estructural
por qué el parser actual no la detectó
```

Si no existe evidencia documental suficiente:

```text
STATUS = UNKNOWN
```

No inventar.

---

# 8. CASO ESPECIAL: AGRUPACIONES EXTINTAS

El reporte anterior mencionó como hipótesis:

```text
tepecano †
ópata †
```

Esto es solamente una HIPÓTESIS.

NO introducir ninguno como agrupación faltante hasta demostrarlo directamente en el PDF.

Si existe en el catálogo:

```text
preservarlo exactamente
```

Si no existe como agrupación:

```text
no introducirlo
```

---

# 9. TERCERA OPERACIÓN — AUDITORÍA DE LAS 364 VARIANTES

El número:

```text
364
```

es necesario pero insuficiente.

Debes generar un inventario completo:

```text
variant_id
family
group
variant_name
page
source_reference
```

Debe existir exactamente:

```text
364
```

registros.

Después:

```text
COUNT = 364
```

y:

```text
DISTINCT(source_page, variant_name, hierarchy)
```

debe permitir reconstruir cada registro.

---

# 10. IDENTIDAD DE REGISTRO

NO uses solamente:

```text
variant_name
```

como identidad.

Una misma denominación podría aparecer en contextos diferentes.

La identidad lógica debe considerar como mínimo:

```text
family
group
variant_name
source_document
source_page
```

Si existe una mejor clave documental, documentarla.

Nunca destruir una posible colisión solamente para satisfacer un índice UNIQUE.

---

# 11. CUARTA OPERACIÓN — AUDITORÍA DE LAS 475 AUTODENOMINACIONES

ESTA PARTE ES CRÍTICA.

El reporte dice:

```text
475 autodenominaciones
364 variantes
```

Eso NO es un error automáticamente.

Puede significar:

```text
1 variante → varias autodenominaciones
```

Por lo tanto NO guardar solamente:

```text
LanguageVariant.Autodenomination
```

si eso destruye información múltiple.

---

# 12. MODELO CORRECTO PARA AUTODENOMINACIONES

Evaluar si se requiere una entidad separada, conceptualmente:

```text
LanguageVariant
       │
       └── LanguageVariantAutodenomination
                  │
                  ├── OriginalText
                  ├── SourcePage
                  ├── SourceSection
                  ├── SourceDocument
                  └── Provenance
```

El nombre exacto de la entidad queda a criterio de la arquitectura existente, pero la regla es obligatoria:

> **Ninguna de las 475 autodenominaciones puede perderse por intentar meterlas en un único campo de una variante.**

Debe poder responderse:

```text
¿Cuántas autodenominaciones tiene esta variante?
¿En qué página aparece cada una?
¿Cuál es el texto original?
¿De qué sección proviene?
```

---

# 13. AUTODENOMINACIÓN ≠ TRADUCCIÓN

No interpretar.

No convertir:

```text
autodenomination
```

en:

```text
SpanishName
EnglishName
Meaning
Translation
```

La Fase 1 únicamente conserva el dato fuente.

---

# 14. APÉNDICE 4

El Apéndice 4 debe auditarse como fuente documental independiente.

El reporte indica:

```text
475 autodenominaciones
correspondientes a
364 variantes
```

Debes extraer la estructura completa.

Crear una relación demostrable:

```text
Autodenominación
        ↓
Variante
        ↓
Agrupación
        ↓
Familia
```

Pero NO asumir que el apéndice reemplaza el contenido principal.

Conservar:

```text
source_section = main_catalog
```

o:

```text
source_section = appendix_4
```

según corresponda.

---

# 15. CONFLICTOS ENTRE CUERPO Y APÉNDICE

Si el cuerpo principal dice:

```text
X
```

y el Apéndice 4 dice:

```text
Y
```

NO resolver automáticamente.

Conservar ambas evidencias:

```text
main_catalog_value = X
appendix_value = Y
```

y registrar:

```text
conflict_status = detected
```

hasta que exista una regla documental explícita.

---

# 16. QUINTA OPERACIÓN — GEOREFERENCIAS

Actualmente:

```text
359 / 364
```

tienen referencia geoestadística extraída.

NO completar las 5 faltantes con fuentes externas.

Primero determinar:

```text
¿realmente no tienen geo?
¿el parser no la detectó?
¿está en otra sección?
¿está en una nota?
```

Si realmente no existe:

```text
NULL + provenance
```

es correcto.

Nunca inventar.

---

# 17. SEXTA OPERACIÓN — CÓDIGOS

El reporte indica:

```text
INALI/ISO = 0/364
```

en el PDF.

Esto NO es un problema del parser.

NO agregar códigos externos todavía.

Si posteriormente se incorporan:

```text
source = INEGI/INALI/ISO/etc.
```

deben quedar claramente separados de los datos del CLIN.

Nunca modificar:

```text
CLIN source record
```

para agregar silenciosamente un código externo.

---

# 18. MODELO DE PROVENANCE

Cada entidad derivada del catálogo debe poder responder:

```text
¿De dónde salió?
```

Como mínimo:

```text
SourceDocument
SourceHash
SourceUrl
SourceVersion
SourcePage
SourceSection
ExtractionDate
ParserVersion
```

No confiar únicamente en:

```text
CreatedAt
```

---

# 19. RAW ES INMUTABLE

El archivo:

```text
catalogs/raw/inali/CLIN_completo.pdf
```

es evidencia patrimonial.

No modificarlo.

No regenerarlo.

No "limpiarlo".

No reemplazarlo silenciosamente.

Si llega una nueva versión:

```text
raw/
  version_001/
  version_002/
```

o una estrategia equivalente.

El archivo anterior nunca debe desaparecer.

---

# 20. AHORA SÍ: REVISAR EL MODELO EF

Una vez resuelto 68/68 y 475/364:

auditar:

```text
LanguageFamily
LanguageGroup
LanguageVariant
```

y cualquier nueva entidad de autodenominación/provenance.

Verificar:

* PK;
* FK;
* índices;
* UNIQUE;
* nullability;
* Unicode;
* longitudes;
* relaciones;
* cascade behavior;
* concurrencia;
* versionado.

---

# 21. REGLA SOBRE UNIQUE

NO crear:

```text
UNIQUE(LanguageVariant.Name)
```

si el documento no garantiza que el nombre sea globalmente único.

Preferir una clave que represente la identidad documental/hierárquica.

Toda restricción UNIQUE debe tener una justificación documentada.

---

# 22. MYSQL REAL

SOLO después de cerrar la auditoría documental.

Configurar MySQL mediante configuración local segura.

NO escribir credenciales reales en:

```text
appsettings.json
```

versionado.

Usar:

```text
appsettings.Development.json
```

si está explícitamente ignorado por Git,

o User Secrets,

o variables de entorno,

según la arquitectura existente.

Verificar primero:

```text
connection succeeds
database exists
migration applies
```

---

# 23. MIGRACIÓN

Aplicar:

```text
dotnet ef database update
```

solamente contra la BD destinada a desarrollo/verificación.

Registrar:

```text
server
database
migration
timestamp
```

No ejecutar contra una BD desconocida.

---

# 24. IMPORTADOR REAL

Crear un pipeline reproducible:

```text
RAW PDF
   ↓
HASH VERIFY
   ↓
PARSER
   ↓
VALIDATION
   ↓
DOMAIN DTO
   ↓
TRANSACTION
   ↓
MYSQL
   ↓
RECONCILIATION
```

No crear un `Seed()` hardcodeado con datos copiados manualmente.

El catálogo debe poder reconstruirse desde:

```text
CLIN_completo.pdf
```

---

# 25. IMPORTACIÓN ATÓMICA

Una importación debe ser:

```text
BEGIN TRANSACTION
        ↓
VALIDATE SOURCE
        ↓
IMPORT
        ↓
COUNT
        ↓
INTEGRITY CHECKS
        ↓
COMMIT
```

Si falla:

```text
ROLLBACK
```

Nunca dejar una BD parcialmente importada y llamarla completa.

---

# 26. RECONCILIACIÓN OBLIGATORIA

Después de importar, demostrar:

```text
SOURCE
11 families
68 groups
364 variants

PARSED
11 families
68 groups
364 variants

MYSQL
11 families
68 groups
364 variants
```

Pero además:

```text
SOURCE RECORDS == PARSED RECORDS
PARSED RECORDS == MYSQL RECORDS
```

No basta con que los COUNT coincidan.

---

# 27. RECONCILIACIÓN POR IDENTIDAD

Crear hashes o claves deterministas de registro.

Ejemplo conceptual:

```text
family
+
group
+
variant
+
source_document
+
source_page
```

Comparar:

```text
SOURCE HASH SET
vs
PARSED HASH SET
vs
MYSQL HASH SET
```

Resultado obligatorio:

```text
missing = 0
extra = 0
changed = 0
```

o explicar documentalmente cada diferencia.

---

# 28. PRUEBA DE UNICODE

Debe existir una prueba real:

```text
PDF
 ↓
parser
 ↓
JSON
 ↓
MySQL
 ↓
SELECT
 ↓
comparación byte/textual
```

Probar caracteres como los ya detectados:

```text
ayöök
ku’ahl
ʔajɤːkʰ
tohono o’otham
ñuju
hñähñú
xnánj nu’ a
```

No basta con verificar que la aplicación "se vea bien".

Debe existir comparación automatizada.

---

# 29. IDEMPOTENCIA

Ejecutar el importador dos veces.

Primera:

```text
11 / 68 / 364
```

Segunda:

```text
11 / 68 / 364
```

La segunda ejecución NO debe crear duplicados.

Demostrar:

```text
before
after
delta
```

---

# 30. VERSIONADO

No sobrescribir el catálogo anterior.

Crear concepto de:

```text
CatalogSource
CatalogVersion
CatalogRecord
```

o equivalente compatible con la arquitectura.

Una versión debe poder marcarse:

```text
Current
```

solo después de validación.

---

# 31. DETECCIÓN DE CAMBIOS

Implementar detección conservadora.

Orden preferente:

```text
HEADERS
ETag
Last-Modified
VERSION METADATA
SHA256
FULL DOWNLOAD
```

No descargar el PDF completo innecesariamente si el servidor permite comprobar que no cambió.

---

# 32. POLLING

No bombardear INALI.

Debe existir configuración explícita:

```text
PollingInterval
Timeout
MaxRetries
Backoff
UserAgent
```

Aplicar exponential backoff ante errores.

Ejemplo conceptual:

```text
retry 1 → espera
retry 2 → espera mayor
retry 3 → espera mayor
...
```

No realizar loops agresivos.

---

# 33. CAMBIO DETECTADO

Si el hash cambia:

```text
OLD VERSION
      +
NEW RAW
      ↓
VALIDATE
      ↓
PARSE
      ↓
RECONCILE
      ↓
REVIEW
      ↓
ACTIVATE
```

NO reemplazar automáticamente la versión vigente antes de validar la nueva.

---

# 34. CUARENTENA

Si el nuevo archivo:

* está corrupto;
* tiene MIME inesperado;
* tiene tamaño anómalo;
* no abre;
* cambia estructura;
* produce conteos inesperados;
* rompe Unicode;
* produce registros imposibles;

debe ir a:

```text
QUARANTINE
```

y la versión vigente debe permanecer intacta.

---

# 35. CAMBIO DE ESQUEMA DEL PDF

Si una futura versión cambia:

```text
columnas
estructura
secciones
nomenclatura
formato
```

NO intentar "arreglarla" silenciosamente.

Registrar:

```text
SCHEMA_CHANGE_DETECTED
```

Preservar RAW.

Detener activación hasta que el parser sea adaptado y probado.

---

# 36. PRUEBAS OBLIGATORIAS

Agregar pruebas para:

### A. Counts

```text
11 families
68 groups
364 variants
```

### B. Unicode

Los casos definidos anteriormente.

### C. Autodenominaciones

```text
475 total
```

si esa cifra corresponde exactamente al apéndice extraído.

### D. Hierarchy

Cada variante pertenece a una agrupación.

Cada agrupación pertenece a una familia.

### E. Provenance

Cada registro tiene referencia documental.

### F. Idempotencia

Importar dos veces no duplica.

### G. Rollback

Provocar error controlado y demostrar:

```text
no partial import
```

### H. Versioning

Importar una versión nueva de prueba sin destruir la anterior.

### I. Hash

Misma fuente → mismo resultado determinista.

---

# 37. ALIN

NO implementar ahora el crawler completo.

Mantener solamente:

```text
ALIN source identified
ALIN homepage RAW preserved
```

El crawler ALIN será otra fase.

No utilizar ALIN para modificar el inventario maestro CLIN.

---

# 38. NO CÓDIGOS EXTERNOS TODAVÍA

No implementar todavía:

```text
ISO
INEGI codes
INPI IDs
```

salvo que ya existan explícitamente en el CLIN fuente.

La incorporación de fuentes externas será un proceso separado con provenance independiente.

---

# 39. INFORME FORENSE FINAL

Al terminar esta intervención genera:

```text
docs/audits/phase-1-inali-final.md
```

Debe contener:

## SOURCE

* URL
* SHA256
* tamaño
* fecha
* páginas

## COUNTS

```text
families = ?
groups = ?
variants = ?
autodenominations = ?
geo references = ?
```

## RECONCILIATION

```text
source
parsed
mysql
```

## RECORD HASH RECONCILIATION

```text
missing
extra
changed
```

## UNICODE

Casos probados.

## PROVENANCE

Cómo regresar desde DB hasta PDF/página.

## IDEMPOTENCY

Resultados.

## VERSIONING

Resultados.

## CHANGE DETECTION

Resultados.

## POLLING/BACKOFF

Resultados.

## TESTS

Cantidad total y resultado real.

---

# 40. NO MENTIR CON LOS ESTADOS

Utiliza exactamente:

```text
VERIFIED
PARTIAL
UNKNOWN
BLOCKED
FAILED
```

No uses:

```text
DONE
COMPLETE
CLOSED
READY
```

si la evidencia no lo demuestra.

---

# 41. CRITERIO DE CIERRE

FASE 1 solamente puede declararse:

```text
CLOSED / VERIFIED
```

si TODO lo siguiente es cierto:

```text
[ ] CLIN RAW preservado
[ ] SHA256 verificado
[ ] 11 familias demostradas
[ ] 68 agrupaciones demostradas
[ ] 364 variantes demostradas
[ ] 475 autodenominaciones reconciliadas, si corresponde al Apéndice 4
[ ] 359/364 geo verificadas o diferencias explicadas
[ ] Unicode round-trip probado
[ ] source → parser reconciliado
[ ] parser → MySQL reconciliado
[ ] MySQL real funcionando
[ ] migración aplicada
[ ] importación transaccional
[ ] rollback probado
[ ] idempotencia probada
[ ] provenance completa
[ ] versionado probado
[ ] cambio de fuente detectable
[ ] polling conservador
[ ] backoff probado
[ ] cuarentena probada
[ ] tests verdes
[ ] informe forense generado
```

Si falta UNA:

```text
FASE 1 = NOT CLOSED
```

---

# 42. CRITERIO DE BLOQUEO

Si no puedes demostrar:

```text
68 grupos
```

detente.

Si no puedes explicar:

```text
475 → 364
```

detente.

Si no puedes demostrar:

```text
SOURCE == PARSED == MYSQL
```

detente.

Si MySQL no está realmente disponible:

```text
BLOCKED
```

No simules la persistencia con SQLite y la presentes como MySQL.

---

# 43. ORDEN EXACTO DE EJECUCIÓN

No improvisar el orden.

```text
1. VERIFY RAW HASH
        ↓
2. AUDIT 11 / 68 / 364
        ↓
3. RESOLVE 67 vs 68
        ↓
4. AUDIT 475 → 364
        ↓
5. AUDIT GEO 359 / 364
        ↓
6. FREEZE PARSED DATASET
        ↓
7. AUDIT EF MODEL
        ↓
8. CONFIGURE REAL MYSQL
        ↓
9. APPLY MIGRATION
        ↓
10. IMPORT TRANSACTIONALLY
        ↓
11. RECONCILE COUNTS
        ↓
12. RECONCILE RECORD IDENTITIES
        ↓
13. UNICODE ROUND-TRIP
        ↓
14. IDEMPOTENCY
        ↓
15. ROLLBACK
        ↓
16. VERSIONING
        ↓
17. CHANGE DETECTION
        ↓
18. POLLING/BACKOFF
        ↓
19. QUARANTINE
        ↓
20. FINAL AUDIT
        ↓
21. ONLY THEN: CLOSED / VERIFIED
```

---

# 44. REGLA FINAL

Este catálogo representa patrimonio lingüístico.

El objetivo NO es:

```text
hacer que los números cuadren
```

El objetivo es:

```text
preservar exactamente lo que dice la fuente
y demostrarlo técnicamente.
```

Si el número esperado dice 68 y el parser encuentra 67:

**NO fabriques el 68.**

Si existen 475 autodenominaciones para 364 variantes:

**NO destruyas las 111 relaciones adicionales.**

Si cinco variantes no tienen georreferencia:

**NO inventes cinco.**

Si el PDF no contiene códigos:

**NO los atribuyas al CLIN.**

Si MySQL no está disponible:

**NO declares persistencia.**

Si una nueva versión cambia la estructura:

**NO la fuerces dentro del parser viejo.**

La prioridad es:

```text
FIDELIDAD
>
TRAZABILIDAD
>
INTEGRIDAD
>
REPRODUCIBILIDAD
>
PERSISTENCIA
>
ACTUALIZACIÓN CONTROLADA
>
FUNCIONALIDAD
```

No avances a traducción, pronunciación, IA, enriquecimiento semántico ni crawler ALIN hasta que esta fase esté demostrablemente cerrada.

## FIN DEL MD
