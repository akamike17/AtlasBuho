# ATLASBUHO — PHASE 1 SURGICAL CORRECTION PASS 2

## Forensic Import Integrity, Ambiguity Control, Quarantine Idempotency, Provenance and Truthful Closure

**Repository:** `akamike17/AtlasBuho`
**Branch:** `review/phase-1-inventario-linguistico`
**Current commit:** `e88de7d2fb0a5592d2d7af6d5d387982b4e543b7`
**Base commit:** `e41a14d5b36ac12f91014c094dbf23e54408bb04`

---

# 0. MISSION

Perform a second forensic audit and surgical correction pass over the current Phase 1 implementation.

The previous agent reported:

> "All 16 surgical corrections implemented and verified against real MySQL."

Do NOT accept that statement as evidence.

The repository must be independently inspected and the implementation must be corrected where the current code still contradicts the stated requirements.

The objective is:

```text
SOURCE PDF
    ↓
DETERMINISTIC EXTRACTION
    ↓
PRESERVED RAW VALUES
    ↓
EXPLICIT MATCHING
    ↓
AMBIGUITY CONTROL
    ↓
PERSISTENT QUARANTINE
    ↓
IDEMPOTENT DATABASE STATE
    ↓
COMPLETE PROVENANCE
    ↓
REPRODUCIBLE VALIDATION
    ↓
TRUTHFUL PHASE CLOSURE
```

This is a preservation/inventory project.

Do not optimize for a green test count.

Optimize for **truthful, reproducible, source-faithful data preservation**.

---

# 1. ABSOLUTE RULES

## 1.1 Do not trust the commit message

The commit message is evidence of intent only.

The following claims require independent proof:

* "all 16 corrections implemented"
* "idempotency proven"
* "real MySQL verified"
* "40/40 pass"
* "zero credentials"
* "all provenance complete"

---

# 1.2 No silent inference

The importer MUST NOT silently select among ambiguous candidates.

Forbidden behavior:

```csharp
First()
FirstOrDefault()
OrderByDescending(...).First()
dictionary[key] = value
if (!ContainsKey(key)) dictionary[key] = value
```

when multiple source candidates represent materially different possible mappings.

If ambiguity exists:

```text
QUARANTINE
```

not:

```text
PICK ONE
```

unless the deterministic selection rule is explicitly supported by the source/documentary semantics.

---

# 1.3 No fake closure

Do NOT mark Phase 1:

```text
CLOSED / VERIFIED
```

unless all closure criteria in this document are demonstrated.

If any required criterion remains unverified:

```text
IN PROGRESS
```

---

# 2. REQUIRED WORKFLOW

Follow exactly:

```text
READ
↓
AUDIT
↓
TRACE
↓
IDENTIFY DEFECTS
↓
SURGICAL EDIT
↓
READ BACK EVERY MODIFIED FILE
↓
VERIFY DIFF
↓
BUILD
↓
TEST
↓
REAL MYSQL TESTS
↓
RE-IMPORT
↓
COMPARE DATABASE STATE
↓
AUDIT REPORT
```

Do not skip read-back verification.

Do not report a test as passed merely because the command exited successfully.

---

# 3. H-103 — AMBIGUITY MUST ACTUALLY BE QUARANTINED

## Current suspected defects

Audit `ImportVariantsAsync()`.

The current implementation contains logic equivalent to:

```csharp
else if (prefixMatches.Count > 1)
{
    var bestMatch = prefixMatches
        .OrderByDescending(kvp => kvp.Key.Length)
        .First();

    groupName = bestMatch.Value;
}
```

This must be reviewed.

Also audit construction of:

```text
spanishNameToAgrupacion
```

especially logic equivalent to:

```csharp
if (!spanishNameToAgrupacion.ContainsKey(spanishName))
{
    spanishNameToAgrupacion[spanishName] = agrupacion;
}
```

This can discard source ambiguity.

---

# 4. REQUIRED H-103 IMPLEMENTATION

Build an explicit candidate model.

For each source key:

```text
SpanishName
    ↓
all matching Appendix4 rows
    ↓
candidate Agrupaciones
    ↓
candidate Families
    ↓
candidate pages
```

If one unique semantic candidate exists:

```text
RESOLVE
```

If multiple candidates remain:

```text
QUARANTINE
```

Persist:

* raw source row
* comparison key
* source page
* source section
* candidate count
* candidate values
* reason
* resolution method

Do NOT discard candidates merely because one happens to appear first.

---

# 5. LONGEST PREFIX IS NOT AUTOMATICALLY A VALID RESOLUTION

A longest-prefix algorithm may be deterministic.

That does NOT automatically make it semantically correct.

If the source contains:

```text
A
A B
A B C
```

and all three can match the same extracted value, document why the longest candidate is authoritative.

If the source/documentary structure does not establish that rule:

```text
QUARANTINE
```

If the rule IS authoritative:

* document it
* test it
* name the resolution method explicitly
* prove no conflicting candidates survive

---

# 6. H-102 — ELIMINATE FALLBACK SOURCE PAGES

Search the entire repository for:

```text
GetValueOrDefault
```

and specifically:

```text
GetValueOrDefault(..., 244)
GetValueOrDefault(..., 30)
GetValueOrDefault(..., any page)
```

Current code contains logic equivalent to:

```csharp
var page = agrupacionToPage.GetValueOrDefault(agrupacion, 244);
```

This contradicts the stated H-102 correction.

Replace fallback behavior with:

```text
EXPLICIT RESOLUTION
or
QUARANTINE
```

Never manufacture provenance.

A page number is evidence, not a default value.

---

# 7. PROVENANCE RULE

Forbidden:

```text
page = 244
```

merely because most Appendix 4 groups happen to be around that page.

Required:

```text
source row
    ↓
actual source page
    ↓
persisted provenance
```

If source page cannot be established:

```text
UNRESOLVED
```

and quarantine where appropriate.

---

# 8. IMPORTQUARANTINE — TRUE IDEMPOTENCY

The new `ImportQuarantine` entity is useful, but the current implementation must be audited for duplicate accumulation.

Current behavior calls:

```text
PersistQuarantineAsync(...)
```

on every import.

A second identical import can therefore generate another quarantine row.

That is NOT strict idempotency.

---

# 9. QUARANTINE IDENTITY

Define a deterministic identity for a quarantine event.

Candidate:

```text
CatalogVersionId
+
SourceDocumentId
+
EntityType
+
SourcePage
+
ComparisonKey
+
Reason/ReasonCode
+
RawDataHash
```

Do not blindly use the entire JSON string unless its serialization is guaranteed deterministic.

Prefer:

```text
RawDataHash = SHA256(canonicalized raw source row)
```

Then define a database uniqueness constraint appropriate to the semantic identity.

The exact key must be justified.

---

# 10. QUARANTINE RE-IMPORT TEST

On a clean database:

```text
IMPORT #1
IMPORT #2
IMPORT #3
```

Compare:

* total quarantine rows
* open quarantine rows
* accepted artifacts
* rejected rows
* resolved rows
* exact identities

Expected:

```text
Import 2 == Import 1
Import 3 == Import 1
```

No duplicate quarantine records.

---

# 11. CATALOGRECORD IDEMPOTENCY

Audit `CreateCatalogRecordsAsync()`.

The current implementation constructs records and adds them without an obvious semantic existence check.

Determine whether repeated import creates:

```text
duplicate CatalogRecords
```

for the same:

```text
CatalogVersion
EntityType
IdentityKey
```

If yes, implement deterministic idempotency.

Recommended semantic identity:

```text
CatalogVersionId
+
EntityType
+
IdentityKey
```

Create a unique database constraint if compatible with the domain model.

Do not rely only on application-level:

```text
AnyAsync()
then
Add()
```

if concurrent execution can occur.

The database must protect the invariant.

---

# 12. CATALOGVERSION SEMANTICS

Audit:

```text
GetOrCreateCatalogVersionForUpdateAsync()
```

Current behavior resets an existing version to:

```text
Importing
```

even if it was already:

```text
Completed
```

Determine whether this is the intended version lifecycle.

The important distinction is:

```text
same source + same parser = same immutable catalog version
```

versus:

```text
same source + same parser = mutable re-import session
```

Pick one explicit domain model.

Do not silently mix both.

If a completed version is immutable:

```text
do not mutate it
```

and treat a re-import as validation of the same deterministic version.

If it is mutable:

document why.

---

# 13. CATALOGRECORDS AND VERSION IMMUTABILITY

A completed catalog version should have a stable record set.

Verify:

```text
Completed version
+
same source
+
same parser
=
same records
```

No record multiplication after repeated execution.

---

# 14. SOURCE DOCUMENT REVALIDATION

Audit:

```text
CreateOrGetSourceDocumentAsync()
```

Current behavior may return immediately when the `SourceDocument` already exists.

That can bypass:

* SourcePage creation
* RawTextHash generation
* page-count verification

if the existing document was created by an older importer version.

Required behavior:

For an existing source document with the same SHA256:

```text
verify document hash
verify page count
verify SourcePages
verify RawTextHash coverage
verify section assignment
```

If pages are incomplete:

```text
repair deterministically
```

or fail explicitly.

Do not silently accept legacy incomplete provenance.

---

# 15. RAW TEXT HASH SEMANTICS

The current implementation normalizes extracted PDF text:

```text
multiple whitespace → single whitespace
trim
```

before hashing.

This is not necessarily wrong.

But the field is called:

```text
RawTextHash
```

Therefore define exactly what is being hashed.

If it is normalized extracted text:

rename/document semantics accordingly, e.g.:

```text
NormalizedTextHash
```

OR explicitly document:

```text
RawTextHash = SHA256(canonicalized extracted page text)
```

Do not call normalized text "raw" without defining the canonicalization.

---

# 16. PAGE COVERAGE

Verify:

```text
PDF physical page count
SourceDocument.TotalPages
SourcePages count
```

Expected documentary coverage:

```text
30–256
= 227 pages
```

But do not merely count rows.

Verify:

```text
30,31,32,...,256
```

with no gaps and no duplicates.

Also verify:

```text
MainCatalog = 30–212
Appendices = 213–256
```

against actual source semantics.

---

# 17. DOCUMENTARY ACCOUNTING — 474 AUTODENOMINATIONS

This section is critical.

Current code combines:

```text
Autodenomination artifacts
+
Group artifacts
```

when calculating autodenomination accounting.

That is incorrect unless the source model explicitly defines those rows as one accounting population.

They are different entity types.

Do NOT mix:

```text
GroupsArtifactsQuarantined
```

into:

```text
Autodenominations valid-row accounting
```

---

# 18. REQUIRED ACCOUNTING MODEL

Maintain separate counters:

```text
Appendix4 total physical rows
Header rows
Group artifacts
Autodenomination artifacts
Valid autodenomination rows
Imported autodenominations
Quarantined valid autodenominations
```

The equation must be explicit.

For example:

```text
ValidAutodenominationRows
=
ImportedAutodenominations
+
ValidAutodenominationsQuarantined
```

And separately:

```text
PhysicalAppendixRows
=
Header
+
Artifacts
+
ValidRows
```

Never make the equations appear correct by subtracting counters belonging to another entity type.

---

# 19. REQUIRED 474 RECONCILIATION TABLE

Produce an actual table from the source dataset:

| Category                            | Count |
| ----------------------------------- | ----: |
| Appendix physical rows              |       |
| Header rows                         |       |
| Group artifacts                     |       |
| Autodenomination artifacts          |       |
| Valid autodenomination rows         |       |
| Imported valid autodenominations    |       |
| Quarantined valid autodenominations |       |
| Accounting difference               |       |

Required:

```text
Accounting difference = 0
```

---

# 20. VARIANT ACCOUNTING

Expected:

```text
364 documentary variants
```

If:

```text
363 imported
1 quarantined
```

then the database/report must explicitly identify the quarantined source row.

Example:

```text
ku'ahl
reason = no authoritative group assignment in source
source page = X
```

Do not merely report:

```text
363 + 1 = 364
```

without evidence.

---

# 21. AUTODENOMINATION MATCHING

Audit every resolution method.

Current methods include concepts such as:

```text
spanish_name_exact
spanish_name_prefix_unique
spanish_name_progressive_prefix_best
spanish_name_suffix
generic_group_name_disambiguated
single_variant_fallback
```

For EACH method determine:

1. Is it deterministic?
2. Is it source-supported?
3. Can it produce a false positive?
4. Can it hide ambiguity?
5. Is the source evidence persisted?
6. Is it tested?

---

# 22. SINGLE-VARIANT FALLBACK

Audit:

```text
single_variant_fallback
```

This is potentially dangerous.

A family having one imported variant does not necessarily mean an unmatched Appendix 4 row belongs to it.

Require explicit documentary justification.

If not justified:

```text
QUARANTINE
```

---

# 23. PROGRESSIVE PREFIX MATCHING

Audit all progressive-prefix logic.

For every match record:

```text
original SpanishName
candidate prefix
candidate variant
candidate count
resolution method
source page
```

If multiple equally plausible candidates remain:

```text
QUARANTINE
```

---

# 24. NO FIRST-CANDIDATE SEMANTICS

Search repository for patterns:

```text
.FirstOrDefault()
.First()
ContainsKey()
dictionary[key] =
```

Review each occurrence in import/reconciliation code.

Not every `FirstOrDefault()` is a defect.

But every one used to resolve source identity must be justified.

Document:

```text
SAFE
```

or:

```text
AMBIGUOUS — FIX REQUIRED
```

---

# 25. REAL MYSQL VALIDATION

Do not rely only on EF InMemory tests.

Run against real MySQL.

Required sequence:

```text
1. clean database
2. apply migrations
3. import #1
4. capture complete database snapshot
5. import #2
6. capture complete database snapshot
7. import #3
8. capture complete database snapshot
```

Compare:

* entity counts
* quarantine counts
* identities
* hashes
* provenance
* CatalogVersion
* CatalogRecords
* timestamps where semantically expected
* status fields

---

# 26. DATABASE SNAPSHOT

Create deterministic comparison queries.

At minimum:

```sql
SELECT COUNT(*) ...
```

for:

```text
LanguageFamilies
LanguageGroups
LanguageVariants
LanguageVariantAutodenominations
SourceDocuments
SourcePages
CatalogSources
CatalogVersions
CatalogRecords
ImportQuarantines
```

Also compute duplicate checks for every natural identity.

---

# 27. DUPLICATE DETECTION

Run explicit SQL duplicate detection.

Examples:

```sql
GROUP BY LanguageFamilyId, Name
HAVING COUNT(*) > 1
```

```sql
GROUP BY LanguageGroupId, Name
HAVING COUNT(*) > 1
```

```sql
GROUP BY LanguageVariantId, Autodenomination, SourcePage
HAVING COUNT(*) > 1
```

```sql
GROUP BY CatalogVersionId, EntityType, IdentityKey
HAVING COUNT(*) > 1
```

And equivalent quarantine identity query.

Expected:

```text
0 unexpected duplicates
```

---

# 28. FOREIGN KEY INTEGRITY

Verify:

```text
Family → Group
Group → Variant
Variant → Autodenomination
SourceDocument → SourcePage
SourceDocument → CatalogRecord
CatalogVersion → CatalogRecord
CatalogVersion → ImportQuarantine
SourceDocument → ImportQuarantine
```

Expected:

```text
0 orphan rows
```

---

# 29. UNICODE

Repeat the Unicode test.

Verify:

```text
JSON → importer → MySQL → SELECT
```

for all relevant autodenominations.

Required:

```text
0 U+FFFD
0 unexpected normalization loss
0 missing Unicode characters
```

Do not only count unique characters.

Also compare complete source strings.

---

# 30. CHARACTER FIDELITY

For all imported source strings, compare:

```text
source representation
vs
persisted representation
```

Distinguish:

```text
original value
normalized comparison key
persisted canonical/display value
```

Normalization is allowed only where explicitly defined.

---

# 31. SECURITY AUDIT

Repeat repository-wide searches for:

```text
password
secret
credential
connection string
UserSecrets
```

Do not report "zero credentials" if historical Git history contains them.

Correct wording must distinguish:

```text
CURRENT TREE CLEAN
```

from:

```text
GIT HISTORY CLEAN
```

If historical secrets existed:

state that clearly and recommend credential rotation if appropriate.

Do not falsify the history result.

---

# 32. TEST SUITE

The test suite must cover at minimum:

### T1

Clean import baseline.

### T2

Second import produces identical semantic state.

### T3

Third import produces identical semantic state.

### T4

Ambiguous mapping is quarantined.

### T5

Duplicate quarantine is prevented.

### T6

Duplicate CatalogRecord is prevented.

### T7

Rollback is atomic.

### T8

Unicode round-trip.

### T9

Page provenance.

### T10

Source hash verification.

### T11

Documentary accounting.

### T12

Foreign-key integrity.

### T13

Version determinism.

### T14

No silent fallback page.

---

# 33. ROLLBACK TEST

Do not leave rollback as "future work."

Run a controlled test against a disposable database.

Force an exception after partial persistence.

Verify all transactional entities return to:

```text
0
```

or their pre-import state.

Restore the code after the temporary fault injection.

The production branch must not contain test-only failure injection.

---

# 34. VERSION DETERMINISM

Verify:

```text
VersionNumber =
SHA256(SourceDocumentHash + ParserVersion)[0..12]
```

using the actual implementation.

Run independently in:

* C#
* SQL or external script

and compare.

Expected:

```text
exact match
```

---

# 35. DOCUMENTATION RECONCILIATION

Audit all Phase 1 documentation.

Especially:

```text
docs/audits/phase-1-final-audit-report.md
docs/audits/phase-1-pass1-source.md
docs/audits/phase-1-pass2-reconciliation.md
docs/audits/phase-1-pass3-mysql.md
docs/audits/phase-1-pass4-integrity.md
```

No document may claim:

```text
CLOSED
VERIFIED
COMPLETE
```

while another authoritative Phase 1 document says:

```text
PENDING
PARTIAL
UNVERIFIED
```

unless the document explicitly describes historical state and gives the superseding evidence.

---

# 36. IMPORTANT: DO NOT DELETE HISTORICAL AUDIT EVIDENCE

Do NOT rewrite history to make previous reports look correct.

If a previous audit was wrong:

document:

```text
Historical finding
Current corrected finding
Reason for correction
Evidence
```

Preserve auditability.

---

# 37. COMMIT MESSAGE CLAIMS

After corrections, update the final commit message/report so that it does not overclaim.

Do not write:

```text
all verified against real MySQL
```

unless the exact MySQL test evidence is available.

Use:

```text
verified locally against MySQL
```

only if actually executed and recorded.

---

# 38. FINAL EVIDENCE MATRIX

Produce:

| Finding                              | Status | Evidence |
| ------------------------------------ | ------ | -------- |
| H-101 RawTextHash                    |        |          |
| H-102 no page fallback               |        |          |
| H-103 ambiguity quarantine           |        |          |
| H-104 original/normalized separation |        |          |
| H-105 persistent quarantine          |        |          |
| H-106 documentary accounting         |        |          |
| H-107 matching evidence              |        |          |
| H-108 source-field propagation       |        |          |
| H-109 family/group provenance        |        |          |
| H-110 identity context               |        |          |
| H-111 idempotency                    |        |          |
| H-112 version state lifecycle        |        |          |
| H-113 source hash revalidation       |        |          |
| H-114 parser version                 |        |          |
| H-116 test matrix                    |        |          |

Use only:

```text
CONFIRMED
PARTIALLY CONFIRMED
UNVERIFIED
FAILED
NOT APPLICABLE
```

---

# 39. REQUIRED NUMERIC RECONCILIATION

The final report must explicitly show:

```text
Families:
11 documentary
11 imported
0 unexplained

Groups:
68 documentary
68 imported
0 unexplained

Variants:
364 documentary
363 imported
1 quarantined
0 unexplained

Autodenominations:
474 valid documentary
X imported
Y valid quarantined
X + Y = 474
```

Then separately:

```text
Appendix artifacts:
X
```

No cross-category subtraction.

---

# 40. FINAL MYSQL EVIDENCE

Record:

```text
MySQL version
database name
migration state
import command
execution timestamp
result
entity counts
duplicate counts
orphan counts
hash results
Unicode results
idempotency comparison
```

Do not expose credentials.

---

# 41. PHASE CLOSURE RULE

Phase 1 may be marked:

```text
CLOSED / VERIFIED
```

ONLY when:

* H-102 is actually fixed.
* H-103 is actually ambiguity-safe.
* Quarantine is persistent AND idempotent.
* CatalogRecords are idempotent.
* Documentary accounting is mathematically correct.
* SourcePages are complete and verified.
* Raw/normalized semantics are explicitly defined.
* Source hash is revalidated.
* Unicode is verified.
* Foreign keys are verified.
* Rollback is tested.
* 3x re-import is tested on clean MySQL.
* No contradictory documentation remains.
* Actual evidence exists for every "verified" claim.

Otherwise:

```text
PHASE 1 — IN PROGRESS
```

---

# 42. FINAL REPORT FORMAT

End the agent report with exactly:

## Confirmed

Only facts directly demonstrated.

## Partially Confirmed

Facts with remaining limitations.

## Unverified

Claims not directly tested.

## Failed

Requirements that still fail.

## Files Changed

Exact paths.

## Build

Exact command + actual output.

## Tests

Exact command + actual result.

## MySQL

Exact validation + actual result.

## Database Counts

Exact numbers.

## Duplicate Audit

Exact numbers.

## Provenance Audit

Exact numbers.

## Phase Status

One of:

```text
CLOSED / VERIFIED
```

or:

```text
IN PROGRESS
```

Never use optimistic wording to imply closure.

---

# 43. FINAL PRINCIPLE

AtlasBuho is intended to preserve linguistic and cultural source material.

Therefore:

```text
An unresolved record is better than a fabricated match.
An explicit quarantine is better than a silent guess.
A documented limitation is better than a false PASS.
A reproducible database state is better than a green unit-test count.
```

The importer must preserve uncertainty rather than erase it.

Do not "make the numbers fit."

Make the numbers **true**.
